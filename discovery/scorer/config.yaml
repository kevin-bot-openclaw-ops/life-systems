# Job Scoring Configuration
# Weights must sum to 1.0

weights:
  remote_match: 0.35        # Remote work is mandatory - high weight
  ai_ml_relevance: 0.30     # Core AI/ML skills match
  seniority_match: 0.20     # Senior+ level match
  salary_match: 0.10        # €150k+ target
  fintech_bonus: 0.05       # Banking/fintech domain bonus

filters:
  salary_floor_eur: 100000  # Minimum acceptable (converted from other currencies)
  remote_only: true         # Reject non-remote listings
